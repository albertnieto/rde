# RDE domain adapter tests (HSP + TSP)

Adapter tests for `rde_domains` entry points. The core suite in `tests/rde/`
must remain runnable without importing this package.

```bash
.venv/bin/python3 -m pytest tests/rde_domains -q
```
